name: 'Setup Linqpad'
description: 'Setup Linqpad and add to PATH for use'
runs:
  using: 'node12'
  main: 'dist/index.js'
inputs:
  version:
    description: 'The LinqPad version to use'
    required: false
    default: '6.x'
